{
  "name": "wellness-backend",
  "version": "1.0.0",
  "description": "Backend API for Wellness Outcomes Platform",

  "engines": {
    "node": "18.x"
  },

  "main": "dist/server.js",

  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },

  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^21.6.1"
  },

  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^18.19.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
